ARM_FPU=neon
MARCH_TUNE="-march=armv7-a -mtune=cortex-a8 -mfpu=${ARM_FPU} -mfloat-abi=hard"
CFLAGS="${CFLAGS} ${MARCH_TUNE}"
CXXFLAGS="${CXXFLAGS} ${MARCH_TUNE}"

USE="${USE} chromeless_tty hardfp legacy_keyboard legacy_power_button neon"
USE="${USE} -opengl opengles serial_use_ttyO0 -tpm"
